/*

var expect = chai.expect;
// var should = chai.should();
var sandbox;

beforeEach(function() {
  sandbox = sinon.sandbox.create();
});

afterEach(function() {
  sandbox.restore();
});

describe('Product', function() {
  var p = new Product("productName", '3.00',5);

  it('should be a function', function() {
    expect(Product).to.be.a('function');    
    expect(p instanceof Product).to.be.true;

  });

  it('should have a name', function() {
    expect(Product).to.have.property('name').with.lengthOf(11);
    expect(p.name).to.equal.to('productName');
  });

  it('should have a price', function() {
    expect(Product).to.have.property('price').with.lengthOf(4);
    expect(p.price).to.equal.to('3.00');
  });

  it('should have a inventory', function() {
    expect(Product).to.have.property('inventory').with.lengthOf(1);
    expect(p.inventory).to.equal.to(3);
  });


}); // end product specs

*/
